# Lua Obfuscator Discord Bot

A Discord bot that obfuscates Lua/Luau code using the unveilr obfuscator.

## Usage
- Type `!obfuscate` and attach a `.lua` or `.txt` file
- Bot will obfuscate and send back the file

## Setup
See deployment instructions
